@require "../../node_modules/jeet/stylus/jeet/index"
// Stylus is being an idiot; can't find this
//@require "../../node_modules/normalize.css/normalize.css"
@require "typography"

body
	background: #eee
	margin: 0

li
	list-style: none

main
	margin: 15px

ul
	margin: 0
	padding: 0

.article-blurb
	gutter = 1
	@media (min-width: 600px)
		column(1/2, gutter: gutter)
	@media (min-width: 900px)
		column(1/3, gutter: gutter)

	article
		padding: 15px
	
	header
		h2()
